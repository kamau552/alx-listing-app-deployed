module.exports = {
    // Commented out the rewrite rule that was causing infinite loops I wroytr about in the other filr
    // async rewrites() {
    // return [
    //     {
    //       source: '/api/properties/:path*',
    //       destination: 'https://alx-listing-app-deployed-hazel.vercel.app/api/properties/:path*'
    //     }
    // ]
    // }
}
